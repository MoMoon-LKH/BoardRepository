<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.momoon.board.token.repository.TokenInfoMapper">

    <insert id="saveTokenInfo">
        INSERT INTO
            tokeninfo (
                memberId,
                tokenValue
            )
        VALUES (
            #{memberId},
            #{tokenValue}
            )
    </insert>
</mapper>